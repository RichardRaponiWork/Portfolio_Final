@model IEnumerable<Portfolio.Models.ProjectTag>

@{
    ViewBag.Title = "ProjectByTag";
}

<h2>ProjectByTag</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Project.ProjectName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tag.TagName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Project.ProjectName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tag.TagName)
        </td>
        <td>
            @*@Html.ActionLink("Edit", "Edit", new { id=item.ProjectTagID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ProjectTagID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ProjectTagID })*@
        </td>
    </tr>
}

</table>




@*foreach (var item in Model) {
<tr>
    <td>

        @Html.DisplayFor(modelitem => item.ProjectSkillLevel)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Project.ProjectName)
    </td>
    <td>
        @Html.ActionLink(item.Skill.SkillName, "ProjectBySkill", new { ID = item.SkillID })

    </td>
    <td>
        @Html.ActionLink("Edit", "Edit", new { id = item.ProjectSkillID }) |
        @Html.ActionLink("Details", "Details", new { id = item.ProjectSkillID }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.ProjectSkillID })
    </td>
</tr>*@




