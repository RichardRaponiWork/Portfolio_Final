@model IEnumerable<Portfolio.Models.ProjectTag>

@{
    ViewBag.Title = "Index";
}

<body id="body4">
    <h2>Index</h2>

    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Project.ProjectName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tag.TagName)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Project.ProjectName)
                </td>
                <td>
                    @Html.ActionLink(item.Tag.TagName, "ProjectByTag", new { ID = item.TagID })
                    @*@Html.DisplayFor(modelItem => item.Tag.TagName)*@
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.ProjectTagID }) |
                    @Html.ActionLink("Details", "Details", new { id = item.ProjectTagID }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.ProjectTagID })
                </td>
            </tr>
        }

    </table>
</body>